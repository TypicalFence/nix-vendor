{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger with Arguments",
            "type": "debugpy",
            "request": "launch",
            "program": "debug.py",
            "console": "integratedTerminal",
            "args": "${command:pickArgs}"
        }
    ]
}